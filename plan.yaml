name: "Demo"
hosts:
  - demo
debug: true
vars:
  message: "mama we made it!!"
tasks:
  - name: "Install Mongo"
    include: "demo/installMongo.yaml" 
    when: 'vars.current_hostname == "192.168.33.11"'
  - name: "Setup hello message"
    include: "demo/helloMessage.yaml"
    when: 'vars.current_hostname == "192.168.33.10"'
